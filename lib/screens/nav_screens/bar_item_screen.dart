import 'package:flutter/material.dart';

class BarItemScreen extends StatelessWidget {
  const BarItemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("Bar item screen")));
  }
}
